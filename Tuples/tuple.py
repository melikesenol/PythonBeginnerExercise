# Tuples are like lists but they are immutable

my_tuple = (1,2,3,4,5)

my_tuple[1] = 'z'   # throws TypeError: 'tuple' object does not support item assignment

print(my_tuple[1])

print(5 in my_tuple)

# cant sort and reverse tuples

# faster than lists

